🤖 GeminiDuck

GeminiDuck — это Telegram-бот, построенный вокруг API Gemini (Google), предназначенный для диалогового взаимодействия, генерации ответов и обработки пользовательских запросов.
Проект ориентирован на простой запуск, изоляцию окружения и удобное развёртывание через Docker.

🚀 Возможности

  💬 Диалоговый Telegram-бот

  🧠 Интеграция с Gemini API

  ⚙️ Конфигурация через переменные окружения

  🐳 Полная поддержка Docker и docker-compose

  📦 Возможность установки как Python-пакета

  🔒 Безопасная работа с токенами (через .env)

🗂 Структура проекта
.
├── gemini_bot.py        # Основная логика Telegram-бота
├── Dockerfile           # Docker-образ приложения
├── docker-compose.yml   # Упрощённый запуск через Docker Compose
├── setup.py             # Установка проекта как пакета
├── requirements.txt     # Python-зависимости
├── README.md            # Документация проекта

⚙️ Установка и запуск
1️⃣ Клонирование репозитория
  git clone https://github.com/nulnul0ne/geminiduck.git
  cd geminiduck

2️⃣ Переменные окружения

Создай файл .env в корне проекта:

  TELEGRAM_BOT_TOKEN=your_telegram_token
  GEMINI_API_KEY=your_gemini_api_key


⚠️ .env не коммитится и хранится только локально.

▶️ Запуск
🔹 Локально (Python)
  python -m venv .venv
  source .venv/bin/activate      # Windows: .venv\Scripts\activate
  pip install -r requirements.txt
  python gemini_bot.py

🔹 Через Docker
  docker-compose up --build


Docker сам:

соберёт образ

подтянет зависимости

запустит бота в изолированном окружении

🧠 Архитектура (кратко)

gemini_bot.py — точка входа и основная логика бота

Gemini API — генерация и обработка ответов

Telegram Bot API — взаимодействие с пользователем

Docker / Compose — изоляция и воспроизводимый запуск

setup.py — подготовка к пакетированию и переиспользованию

Проект спроектирован так, чтобы его было легко:

расширять,

контейнеризировать,

переносить между средами.

🛡 Безопасность

Все чувствительные данные выносятся в .env

Репозиторий не содержит токенов или ключей

Рекомендуется использовать отдельные ключи для dev/prod

📌 Статус проекта

Проект находится в активной разработке.
Базовая функциональность стабильна и готова к использованию.

📄 Лицензия

MIT
